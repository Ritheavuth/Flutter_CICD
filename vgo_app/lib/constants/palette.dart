import 'package:flutter/material.dart';

class Palette {
  static const primaryColor = Color.fromRGBO(26, 103, 135, 1);
  // static const primaryColor = Color.fromRGBO(81, 136, 94, 1);
  // static const primaryColor = Color.fromRGBO(156, 216, 155, 1);
  static const accentColor = Colors.white;
  static const textColor = Colors.black;
  static const secondTextColor = Color.fromRGBO(196, 196, 196, 1);
  static const invalidColor = Color.fromRGBO(238, 92, 92, 1);

  static const defaultPadding = 20.0;
  static const defaultTextSize = 12.0;
}
